enum 🎛️Default { //MARK: Work in progress
    enum FontSize {
        enum SystemFamily {
            static let title = 24
            static let comment = 14
        }
        enum AccessoryFamily {
            static let title = 18
            static let comment = 10
        }
    }
}
