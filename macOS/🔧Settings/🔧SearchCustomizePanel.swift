import SwiftUI

struct 🔧SearchCustomizePanel: View {
    var body: some View {
        Form {
            Text("Placeholder")
        }
        .padding(24)
        .tabItem {
            Label("Search", systemImage: "magnifyingglass")
        }
    }
}
