import SwiftUI

struct 📥NotSupportMultiLineTextInNoteSection: View {
    var body: some View {
        Section {
            Text("Not support multi line text in note.")
                .foregroundStyle(.secondary)
        }
    }
}
